export default function <%= className %>(context, payload, callback) {

  // Inform the store that loading is starting
  //context.dispatch('SAMPLE_ITEM_LOADING', { loading: true });

  // First parameter should call the service by its name that was defined inside the file
  //context.service.read('sampleItem', {id: payload.get('params').get('id')}, {}, (err, result) => {

    // Update stores with new result
    //context.dispatch('SAMPLE_ITEM_LOAD', { sample: result });

    // Inform the store that loading is done
    //context.dispatch('SAMPLE_ITEM_LOADING', {loading: false});

    // End the action
    //callback(err);
  //});
}
